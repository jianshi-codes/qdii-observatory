# Third-party notices

Python and frontend dependencies retain their own licenses. The authoritative dependency graph is `pyproject.toml` and `frontend/pnpm-lock.yaml`; distributors should generate and review a dependency license inventory for each release.

No third-party fund report, market-data dataset, or complete provider response is bundled as release data.
