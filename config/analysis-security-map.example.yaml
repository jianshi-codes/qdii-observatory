# Copy to analysis-security-map.local.yaml. Local entries take priority over the
# public deterministic/manual mapping baseline and are ignored by Git.
version: 1
mappings:
  - match:
      security_code_raw: EXAMPLE-US
      market: US
    symbol: EXAMPLE
    currency: USD
    reason: Synthetic example; replace it with a verified security identifier mapping.
