# 安全策略

请不要在公开 Issue 中提交 API key、cookie、token、真实持仓、数据库 dump 或包含个人信息的日志。

## 支持范围

安全修复只面向默认分支的最新提交和最新发布的 `0.1.x` 版本。alpha 期间不承诺为旧提交提供补丁；复现前请先确认问题仍存在于最新版本。

## 报告漏洞

公开发布前必须启用 GitHub Private Vulnerability Reporting，并在仓库主页提供有效的维护者私密联系方式。启用后请优先使用仓库的 **Security → Report a vulnerability**；如果该入口不可用，请通过仓库主页列出的私密联系方式报告，不要在公开 Issue 披露漏洞细节。报告应包含受影响版本、风险、最小复现步骤和建议修复，避免附带真实账户数据。

维护者会确认收件、评估影响并协调修复与披露时间。安全报告不保证赏金。
